{
  "name": "anyattend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/backend",
    "apps/pwa"
  ],
  "scripts": {
    "dev:backend": "npm --workspace apps/backend run dev",
    "dev:pwa": "npm --workspace apps/pwa run dev",
    "build": "npm run build -w apps/backend && npm run build -w apps/pwa",
    "lint": "npm run lint -w apps/backend && npm run lint -w apps/pwa"
  }
}
